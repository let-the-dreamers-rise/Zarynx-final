{
  "name": "zarynx-contracts",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "node": "hardhat node",
    "deploy:sepolia": "hardhat run scripts/deploy-all.js --network sepolia",
    "deploy:base-sepolia": "hardhat run scripts/deploy-all.js --network baseSepolia",
    "deploy:status-sepolia": "hardhat run scripts/deploy-status.js --network statusSepolia"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox-mocha-ethers": "^3.0.3",
    "@openzeppelin/contracts": "^4.9.6",
    "chai": "^5.2.0",
    "dotenv": "^17.2.0",
    "ethers": "^6.15.0",
    "hardhat": "^3.1.12"
  }
}
